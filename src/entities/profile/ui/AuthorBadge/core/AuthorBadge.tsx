import { IAuthorBadgeProps } from '../types/IAuthorBadgeProps'
import { useAuthorBadge } from './useAuthorBadge'

const AuthorBadge = (props: IAuthorBadgeProps) => {
  const {} = useAuthorBadge(props)

  return <></>
}

export { AuthorBadge }

import { Skeleton } from "@shared/ui/atoms"
import { legalLoadingStyles as st } from "@appData/legal"

const RulesLoading = () => {
  return <Skeleton className={st.root} />
}

export default RulesLoading
